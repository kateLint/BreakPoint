{
  "name": "breakpoint-realtime-worker",
  "version": "1.0.0",
  "description": "Real-time multiplayer backend for BreakPoint using Cloudflare Durable Objects",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.91.0"
  }
}
